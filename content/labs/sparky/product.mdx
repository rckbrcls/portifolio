## A private place for what matters

Sparky brings scheduled Memories, notes, checklists, links, photos, and files into a desktop workspace designed to keep personal context close without requiring an account or hosted backend.

## Four views, one calm workflow

Calendar keeps scheduled Memories visible across day and month views. Mind organizes them into nested areas. Focus runs configurable work and break sessions, while Me summarizes weekly completion activity and rhythm.

## Local by design

Sparky stores its main data with SwiftData and keeps attachment payloads in the Mac's Application Support directory. It does not require an account, custom backend, analytics, or advertising.

## Install on macOS

Available now for macOS 26 or later.

<LabInstallCommand command="curl -fsSL https://rckbrcls.com/api/sparky/install | bash" />

The public installer resolves the latest universal macOS release, installs `Sparky.app`, and uses [GitHub Releases](https://github.com/rckbrcls/sparky/releases/latest) as its distribution source. Its implementation is available in [`scripts/install.sh`](https://github.com/rckbrcls/sparky/blob/master/scripts/install.sh).

A mobile version is coming soon.
